export enum Gender {
	'male',
	'female',
}
export type Education = {
	faculty: string;
	school: string;
};
export type CurrentJob = {
	position: string;
	company: string;
};
export enum Role {
	'admin',
	'user',
	'guest',
}
export enum PostStatus {
	'pending',
	'approved',
}
